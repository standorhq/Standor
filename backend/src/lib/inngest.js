import { Inngest } from "inngest";

export const inngest = new Inngest({ id: "standor" });

// No Clerk webhook functions needed — user creation handled in authController
export const functions = [];
