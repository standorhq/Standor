services:
  - type: web
    name: standor-backend
    env: docker
    rootDir: app/backend
    dockerfilePath: Dockerfile
    region: oregon
    autoDeploy: true
    healthCheckPath: /health
    plan: free
    # Do NOT store secrets here. Configure env vars in Render dashboard after creation.
